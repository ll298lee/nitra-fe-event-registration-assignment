# Project guidance

This is a **plain-JavaScript** Quasar/Vue 3 project — do **not** introduce TypeScript (`lang="ts"`, typed `defineProps`/`defineEmits`, `.ts` files); use runtime prop declarations instead. The installed Vue/UnoCSS skills lean TypeScript in their examples — translate their patterns to plain JS.
