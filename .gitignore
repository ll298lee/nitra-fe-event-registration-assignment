node_modules
dist
.quasar
*.local
.DS_Store

# Claude Code skills (local tooling — installed skill contents stay local;
# skills-lock.json is tracked so the skill set is reproducible)
.agents/
.claude/
tmp.md

# Yarn (Berry)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*
